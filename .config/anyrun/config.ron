Config(
  x: Fraction(0.5),
  y: Fraction(0.45),
  width: Fraction(0.5),
  height: Fraction(0.5),
  hide_icons: false,
  ignore_exclusive_zones: false,
  layer: Overlay,
  hide_plugin_info: true,
  close_on_click: true,
  show_results_immediately: false,
  max_entries: None,

  plugins: [
  //"libhyprwin.so",
    "libapplications.so",
  //"libdictionary.so",
    "libwebsearch.so",
  ],
)
